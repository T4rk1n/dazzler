import React from 'react';
import {PolarRadiusAxisProps} from 'recharts';
import {HtmlOmittedProps} from '../../../commons/js/types';

type Props = Omit<PolarRadiusAxisProps, HtmlOmittedProps>;

const PolarRadiusAxis = (_: Props) => <></>;

export default PolarRadiusAxis;
