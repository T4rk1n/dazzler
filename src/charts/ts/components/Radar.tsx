import React from 'react';
import {RadarProps} from 'recharts';
import {HtmlOmittedProps} from '../../../commons/js/types';

type Props = Omit<RadarProps, HtmlOmittedProps>;

const Radar = (_: Props) => <></>;

export default Radar;
